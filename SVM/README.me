run.sh will run three versions of SVM: primal (stochastic subgradient descent), dual scipy.optimize.minimize, and dual with kernel trick. 
It will print out the type of SVM followed by average train and test errors along with type specific data such as the learned parameters for the primal. 
